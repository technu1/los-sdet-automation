package com.bank.los.steps;

import io.cucumber.java.After;
import io.cucumber.java.Before;

public class Hooks {
  @Before public void before() { /* seed/mocks if needed */ }
  @After  public void after()  { /* attach logs if needed  */ }
}
